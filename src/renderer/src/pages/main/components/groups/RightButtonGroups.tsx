import { RIGHT_MENU_WIDTH } from '@/utils/constants'
import { Box } from '@mui/material'

const RightButtonGroups = () => {
  return <Box sx={{ bgcolor: 'grey.50', width: RIGHT_MENU_WIDTH, height: '100%' }}></Box>
}

export default RightButtonGroups
